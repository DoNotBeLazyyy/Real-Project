export interface StudentProps {
    address?: string,
    age?: number,
    email?: string,
    firstName?: string,
    id: string,
    lastName?: string,
    program?: string,
    sex?: string,
    yearLevel?: string
}