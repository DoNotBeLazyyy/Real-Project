export interface StudentProps {
    studentId?: number,
    address?: string,
    age?: number,
    email?: string,
    firstName?: string,
    lastName?: string,
    program?: string,
    sex?: string,
    studentNumber: string,
    yearLevel?: string
}